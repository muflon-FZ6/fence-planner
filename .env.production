# Public site origin for sitemap, canonicals, and JSON-LD.
# Safe to commit — this is not a secret. Hostinger may not inject
# dashboard env vars during `next build`, so this file ensures the
# production build always has an absolute HTTPS origin.
NEXT_PUBLIC_SITE_URL=https://fenceblueprint.com
