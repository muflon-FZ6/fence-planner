# Production origin for canonicals, sitemap, robots, and JSON-LD.
# Set this in Hostinger (Node.js app → Environment variables), not only locally.
NEXT_PUBLIC_SITE_URL=https://fenceblueprint.com

# Leave unset/false until real AdSense units are wired. Labeled empty ad
# placeholders are not allowed under AdSense policy.
# NEXT_PUBLIC_ADS_ENABLED=true
