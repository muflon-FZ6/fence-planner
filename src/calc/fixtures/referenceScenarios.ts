/**
 * Test re-exports — production builders live in `@/domain/referenceScenarios`.
 */
export {
  buildFpRs01,
  buildFpRs02,
  buildFpRs03,
  buildFpRs04,
  buildFpRs05,
  buildFpRs06,
  buildReferenceScenario,
  fpRs05ConcreteInputs,
  REFERENCE_SCENARIO_SERIALIZATION,
  REFERENCE_EXAMPLE_NOTICE,
  REFERENCE_SCENARIOS,
  examplePlannerHref,
  type ReferenceScenarioId,
} from "@/domain/referenceScenarios";
